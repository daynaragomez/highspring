I need your help implementing Step 5.4 of my ASP.NET Core 8 Razor Pages ecommerce demo.

IMPORTANT:
- Do NOT change any Razor Pages or their behavior.
- Do NOT change any data-testid attributes.
- Do NOT refactor unrelated files.
- Do NOT add public “test reset” HTTP endpoints.
- Scenario switching must happen outside the web app (via Docker/DB seeding).
- The app must continue to run with docker compose.
- Provide full contents for any new or modified files.
- Do not explain — only output file paths and their contents.

Goal:
Add deterministic DB scenario switching for E2E runs using:
- Base seed SQL
- Scenario delta SQL
- A one-shot Docker “seed” service that applies base + scenario deltas
- Scenario selected via environment variable SCENARIO

Current context:
- docker-compose.yml has services: web, db
- EF Core + migrations already exist (Products table, etc.)
- Products are seeded currently via code (we will stop code seeding in this step)
- DB is PostgreSQL

--------------------------------------------------
1) Move seeding from code to SQL
--------------------------------------------------

Modify Program.cs to:
- Remove/disable any code-based product seeding logic added in Step 5.2.
- Keep migrations behavior as-is (do not auto-run migrations unless already done).
- The web app must start without trying to insert seed data.

--------------------------------------------------
2) Add SQL seed files
--------------------------------------------------

Create folder structure at repo root:

/db/base/001_base_seed.sql
/db/scenarios/outofstock.sql
/db/scenarios/discounts.sql

Base seed requirements (001_base_seed.sql):
- Insert at least 8 products with deterministic IDs (explicit IDs) so tests can rely on them.
- Columns: id, name, price, stock, created_utc (or CreatedUtc matching your schema naming).
- Use UPSERT so the script is idempotent:
  - ON CONFLICT (id) DO UPDATE ...
- Ensure product IDs are stable (e.g., 1..8).

Scenario delta requirements:
- outofstock.sql:
  - Set stock=0 for at least 2 specific product IDs.
- discounts.sql:
  - Reduce price by a fixed amount or percent for at least 2 specific product IDs.
  - Keep it deterministic.

IMPORTANT:
- SQL must match the actual table/column names produced by EF Core migrations.
- If EF created quoted PascalCase names, use the correct quoting.
- You may inspect the model conventions and choose correct identifiers.
- If necessary, adjust EF mappings minimally (in AppDbContext OnModelCreating) ONLY to ensure predictable snake_case or consistent naming — but avoid changing existing schema unless unavoidable.

--------------------------------------------------
3) Add a seed runner container/service
--------------------------------------------------

Update docker-compose.yml to add a new one-shot service named "seed" that:
- Uses an official Postgres image (same major version as db)
- Mounts the /db folder into the container read-only
- Waits for Postgres readiness
- Applies:
  a) /db/base/001_base_seed.sql
  b) /db/scenarios/${SCENARIO}.sql if SCENARIO is set and a matching file exists
- Exits with non-zero code if the base seed fails
- Should run before web starts (web depends_on seed completing successfully)

Constraints:
- Do not expose Postgres port to host.
- Use environment variables in compose for DB credentials.
- SCENARIO should default to "default" (meaning: only base seed runs, no scenario delta).

Implementation hint:
- Use psql with connection string like:
  psql "host=db port=5432 dbname=... user=... password=..." -f <file>
- Use a simple shell command in compose to run scripts.

--------------------------------------------------
4) Define “default” behavior
--------------------------------------------------

If SCENARIO is empty, "default", or the scenario file does not exist:
- Only base seed should run.
- The seed container should still succeed.

--------------------------------------------------
Acceptance Criteria
--------------------------------------------------

After implementation:

A) Default run:
- docker compose up --build
- /products shows the deterministic seeded products (IDs stable)
- No code seeding occurs

B) Scenario run:
- SCENARIO=outofstock docker compose up --build
- The specified products show stock=0 in DB (even if UI does not show stock yet, verify via DB behavior if needed)

- SCENARIO=discounts docker compose up --build
- Prices for specified products are discounted in DB and visible on /products

C) Seed service:
- Runs once and exits
- Web starts after seeding completes

--------------------------------------------------
Output Format
--------------------------------------------------

Provide full contents for all created/modified files:
- /src/DemoShop.Web/Program.cs (modified to remove code seeding)
- /docker-compose.yml (modified to add seed service + dependencies)
- /db/base/001_base_seed.sql
- /db/scenarios/outofstock.sql
- /db/scenarios/discounts.sql
- Any other file you must modify for schema naming consistency (only if required)

Do not explain. Only output file paths and their full contents.