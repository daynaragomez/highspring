I need your help implementing Step 2 of my ASP.NET Core 8 Razor Pages ecommerce demo.

IMPORTANT:
- Do NOT introduce database or EF Core.
- Use in-memory data + session storage only.
- Do NOT refactor unrelated files.
- Keep all existing data-testid attributes unchanged.
- Provide full contents for any new or modified files.
- Do not explain anything — only output files and contents.

Goal:
Implement a Product Details page and basic Add to Cart functionality using ASP.NET Core session storage.

--------------------------------------------------
1) Create Product Details Page
--------------------------------------------------

Route:
  /products/{id}

Files:
  /src/DemoShop.Web/Pages/ProductDetails.cshtml
  /src/DemoShop.Web/Pages/ProductDetails.cshtml.cs

Requirements:

- Accept integer id from route.
- Use the SAME hardcoded product list logic used in Products page.
- If product not found → return NotFound().

Display the following:

- <h1>Product Details</h1>
  data-testid="product-details-page-title"

- Product name
  data-testid="product-details-name"

- Product price
  data-testid="product-details-price"

- Quantity input (type=number, min=1, default=1)
  data-testid="quantity-input"

- Add to Cart button
  data-testid="add-to-cart-button"

--------------------------------------------------
2) Enable Session (if not already enabled)
--------------------------------------------------

Modify Program.cs to:

- Add services.AddSession()
- Add services.AddDistributedMemoryCache() if required
- Add app.UseSession()

Keep it minimal.

--------------------------------------------------
3) Implement Add to Cart Logic
--------------------------------------------------

Cart storage rules:

- Store cart in session as JSON.
- Session key: "CART"
- Cart structure:
    List<CartItem>

CartItem fields:
    int ProductId
    string Name
    decimal Price
    int Quantity

Behavior:

- On POST (Add to Cart):
    - Validate Quantity >= 1
    - If invalid → return Page() with validation error.
    - If product already in cart → increase Quantity.
    - If not → add new CartItem.
- After successful add → Redirect to /cart
  (Cart page does NOT need to exist yet.)

--------------------------------------------------
4) Validation
--------------------------------------------------

- Quantity must be >= 1
- If invalid → show ModelState error above form

--------------------------------------------------
Acceptance Criteria
--------------------------------------------------

After implementation:

- Visit:
  http://localhost:8080/products
- Click View → goes to /products/{id}
- Enter quantity
- Click Add to Cart
- Redirect happens to /cart (even if page not implemented yet)
- Session contains cart JSON

--------------------------------------------------
Output Format
--------------------------------------------------

Provide full contents of:

- ProductDetails.cshtml
- ProductDetails.cshtml.cs
- Program.cs (if modified)

Do not explain. Only output file paths and their full contents.