Introduce minimal styling using Bootstrap (via CDN) and render the /products page using Bootstrap cards/layout, without changing behavior or selectors.

Requirements:

1) Add Bootstrap via CDN in the shared layout:
   - Modify the main layout file (likely: /src/DemoShop.Web/Pages/Shared/_Layout.cshtml)
   - Include Bootstrap 5 CSS via CDN in the <head>.
   - Include Bootstrap 5 JS bundle via CDN before </body>.
   - Do not install packages; CDN only.

2) Wrap the main content in a Bootstrap container:
   - Ensure the layout wraps the page body content in:
     <main class="container py-4">
       @RenderBody()
     </main>

3) Update the /products page to use Bootstrap cards:
   - Modify /src/DemoShop.Web/Pages/Products.cshtml only.
   - Render product list using Bootstrap grid:
     - Use .row and responsive columns (e.g. col-12 col-md-6 col-lg-4).
   - Each product should appear as a Bootstrap card:
     - card, card-body, card-title, etc.
   - Keep all existing data-testid attributes EXACTLY the same as currently implemented:
     - products-page-title
     - product-card-<id>
     - product-name-<id>
     - product-price-<id>
     - view-product-<id>

4) Ensure navigation still works:
   - Keep existing navbar link to /products and data-testid="nav-products" unchanged.

Acceptance criteria:
- After changes, I can run:
  docker compose up --build
- Visit:
  http://localhost:8080/products
  and see the products displayed in a Bootstrap card layout.
- All data-testid attributes remain exactly as they were (no renaming, no removal).
- No backend logic or PageModel code changes beyond what is necessary for rendering.

Output format:
- Provide full contents for:
  - /src/DemoShop.Web/Pages/Shared/_Layout.cshtml (or the actual layout file you modified)
  - /src/DemoShop.Web/Pages/Products.cshtml
- Do not explain. Only output the files and their contents.