I need your help implementing the next step of my ASP.NET Core 8 Razor Pages ecommerce demo.

IMPORTANT:
- Do NOT refactor unrelated files.
- Do NOT introduce database or EF Core yet.
- Keep everything simple and clean.
- Provide full file contents for any new or modified files.

Goal of this step:
Create a /products page that displays a small static product list with stable data-testid attributes for future Selenium testing.

Requirements:

1) Add a new Razor Page:
   Route: /products
   File location: /src/DemoShop.Web/Pages/Products.cshtml
   PageModel: Products.cshtml.cs

2) The page must display a hardcoded list of at least 5 products.
   Each product must contain:
   - Id (int)
   - Name (string)
   - Price (decimal)

   Hardcode the list inside the PageModel (no DB yet).

3) The HTML must render each product as a card/div with the following data-testid attributes:

   Product wrapper:
   data-testid="product-card-<id>"

   Product name:
   data-testid="product-name-<id>"

   Product price:
   data-testid="product-price-<id>"

   View link:
   data-testid="view-product-<id>"

   The View link can point to:
   /products/{id}
   (Even if the details page does not exist yet.)

4) Add a navigation link in the main layout (likely _Layout.cshtml) to the Products page.

   The nav link must include:
   data-testid="nav-products"

   The link text should be:
   Products

5) The Products page must include:
   - <h1>Products</h1>
   - data-testid="products-page-title"

6) The page must be reachable at:
   http://localhost:8080/products

7) Do not introduce styling frameworks yet unless already present.
   Simple HTML structure is fine.

Output format:
- Provide full contents for:
  - Products.cshtml
  - Products.cshtml.cs
  - Any modified layout file
- Keep everything minimal and clean.
- Do not explain.