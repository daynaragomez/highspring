I need your help implementing Step 3 of my ASP.NET Core 8 Razor Pages ecommerce demo.

IMPORTANT:
- Do NOT introduce database or EF Core.
- Continue using session storage only.
- Do NOT refactor unrelated files.
- Reuse the existing CartItem structure already stored in session.
- Keep all existing data-testid attributes unchanged.
- Provide full contents for any new or modified files.
- Do not explain anything — only output files and contents.

Goal:
Create the Cart page with the ability to:
- View cart items
- Update quantity
- Remove items
- Display totals

--------------------------------------------------
1) Create Cart Page
--------------------------------------------------

Route:
  /cart

Files:
  /src/DemoShop.Web/Pages/Cart.cshtml
  /src/DemoShop.Web/Pages/Cart.cshtml.cs

--------------------------------------------------
2) Load Cart From Session
--------------------------------------------------

- Read session key "CART"
- Deserialize into List<CartItem>
- If null → treat as empty list
- Do NOT create new cart automatically in session unless modified

--------------------------------------------------
3) Display Requirements
--------------------------------------------------

If cart is empty:

- Display message:
  "Your cart is empty."
- data-testid="empty-cart-message"

If cart has items:

For each item display:

- Wrapper row:
  data-testid="cart-item-<productId>"

- Product name
  data-testid="cart-item-name-<productId>"

- Price
  data-testid="cart-item-price-<productId>"

- Quantity input (number, min=1)
  data-testid="cart-quantity-<productId>"

- Line total (Price * Quantity)
  data-testid="cart-line-total-<productId>"

- Remove button
  data-testid="remove-item-<productId>"

At bottom:

- Cart subtotal (sum of line totals)
  data-testid="cart-subtotal"

- Button: "Proceed to Checkout"
  data-testid="proceed-to-checkout-button"
  (Link to /checkout — page not implemented yet)

--------------------------------------------------
4) Update Quantity
--------------------------------------------------

Behavior:

- Updating quantity submits form (POST)
- Quantity must be >= 1
- If invalid → show ModelState error
- Update session
- Redirect to GET /cart

--------------------------------------------------
5) Remove Item
--------------------------------------------------

Behavior:

- Remove button submits POST
- Remove item from list
- Update session
- Redirect to GET /cart

--------------------------------------------------
6) Totals
--------------------------------------------------

- Line total = Price * Quantity
- Subtotal = sum of all line totals
- Display subtotal formatted as currency

--------------------------------------------------
7) Session Update Rules
--------------------------------------------------

After update/remove:
- Serialize cart back into session as JSON
- If cart becomes empty → remove "CART" key from session

--------------------------------------------------
Acceptance Criteria
--------------------------------------------------

After implementation:

1) Add items from Product Details page
2) Redirect to /cart
3) Cart displays correct items and totals
4) Updating quantity changes subtotal
5) Removing item updates cart
6) When all items removed → empty message appears

--------------------------------------------------
Output Format
--------------------------------------------------

Provide full contents of:

- Cart.cshtml
- Cart.cshtml.cs

If any shared model/class needed (e.g. CartItem) must be moved to shared location, provide that file too.

Do not explain. Only output file paths and their full contents.