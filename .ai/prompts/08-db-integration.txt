I need your help implementing Step 5.3 of my ASP.NET Core 8 Razor Pages ecommerce demo.

IMPORTANT:
- Do NOT change routes.
- Do NOT change any existing data-testid attributes.
- Do NOT change the HTML structure more than necessary.
- Do NOT refactor unrelated files.
- Continue using session cart and session order logic (no DB for cart/orders yet).
- The app must continue to run with docker compose.
- Provide full contents for any modified files.
- Do not explain — only output file paths and file contents.

Goal:
Switch the Products listing and Product Details pages to load product data from PostgreSQL via EF Core, while keeping the UI markup and selectors identical.

Assumptions:
- EF Core is wired with AppDbContext
- Product entity exists and Products table is seeded (Step 5.2)

--------------------------------------------------
1) Replace hardcoded product list on /products
--------------------------------------------------

Modify:
  /src/DemoShop.Web/Pages/Products.cshtml.cs

Requirements:
- Inject AppDbContext (constructor injection).
- Load products from DbContext.Products ordered by Id.
- Map to the same view model shape currently used by the page (Id, Name, Price).
- Keep the existing Products.cshtml markup unchanged (or only minimal edits if required for compilation).
- Ensure all existing data-testid remain exactly the same.

--------------------------------------------------
2) Replace hardcoded product lookup on /products/{id}
--------------------------------------------------

Modify:
  /src/DemoShop.Web/Pages/ProductDetails.cshtml.cs

Requirements:
- Inject AppDbContext.
- Load product by Id from DbContext.Products.
- If product not found → NotFound().
- Use the DB product for display and for Add-to-Cart (Name, Price, ProductId).
- Keep the existing ProductDetails.cshtml markup unchanged (or only minimal edits if required for compilation).
- Ensure all existing data-testid remain exactly the same.

--------------------------------------------------
3) Preserve Cart Contract
--------------------------------------------------

- CartItem stored in session must remain compatible with existing JSON.
- Do not change CartItem property names or types.
- Add-to-cart behavior remains identical.

--------------------------------------------------
4) No new DB features
--------------------------------------------------

- Do NOT add new entities or migrations.
- Do NOT change seeding logic.
- Do NOT persist cart or orders to DB yet.

--------------------------------------------------
Acceptance Criteria
--------------------------------------------------

After implementation:
- docker compose up --build works
- /products shows seeded DB products
- /products/{id} shows product details from DB
- Add to cart still works and redirects to /cart
- All existing data-testid attributes remain unchanged

--------------------------------------------------
Output Format
--------------------------------------------------

Provide full contents for any modified files, likely:
- /src/DemoShop.Web/Pages/Products.cshtml.cs
- /src/DemoShop.Web/Pages/ProductDetails.cshtml.cs
(and any other file you must modify to compile)

Do not explain. Only output file paths and their full contents.
